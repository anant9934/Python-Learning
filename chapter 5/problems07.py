s = {8, 7, 12, "anant", [1, 2, 3]}
# can we change the value of list in set?
# No, we cannot change the value of a list in a set because lists are mutable and
# sets require their elements to be immutable. If we try to add a list to a set,
# it will raise a TypeError.

